import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liftlogger.app',
  appName: 'LiftLogger',
  webDir: '../../dist/apps/mobile/browser',
};

export default config;
